@page
@model movietheatre.Pages.OrderHeaders.IndexModel

@{
    ViewData["Title"] = "Index";
}

<!-- derrived from OrderHeader model -->

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].city)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].state)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].cardnumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].orderDetailsID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderHeader) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.customer.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.city)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.state)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cardnumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.orderDetailsID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
